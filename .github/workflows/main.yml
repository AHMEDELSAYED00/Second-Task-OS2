on:
  push:
    branches:
      - master





jobs:
  build:
    runs-on: ubuntu-latest

    steps: 

      - uses: actions/checkout@v3

      - name: 'Login to Docker hub'
        run: docker login --username ${{ secrets.DOCKER_USERNAME }}  --password dckr_pat_R1wMotE1gci9OJEnAHMGzw-aHUs
