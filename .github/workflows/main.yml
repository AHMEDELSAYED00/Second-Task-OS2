on:
  push:
    branches:
      - master





jobs:
  build:
    runs-on: ubuntu-latest

    steps: 

      - uses: actions/checkout@v3

      - name: 'Login to Docker hub'
        run: docker login --username ${{ secrets.DOCKER_USERNAME }}  --password dckr_pat_ul5pvD4Rf2pMuneyunXxNAJ_pi8