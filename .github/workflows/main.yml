on:
  push:
    branches:
      - master





jobs:
  build:
    runs-on: ubuntu-latest

    steps: 

      - uses: actions/checkout@v3

      - name: 'Login to docker hub'
        run: docker login --username ahmedelsayed01  --password dckr_pat_GWWbWsSPo86lEnbd5N2YKN2v2dg


