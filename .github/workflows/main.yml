on:
  push:
    branches:
      - master





jobs:
  build:
    runs-on: ubuntu-latest

    steps: 

      - uses: actions/checkout@v3

      - name: 'Login to docker hub'
        run: docker login --username ${{ decrets.DOCKER_USERNAME}}  --password ${{ secrets.DOCKER_TOKEN}}


